---
import { getPersonalLinks } from "../data/personal-links.data";
import {
  ButtonPropsColor,
  ButtonTextSize,
  IconSide,
} from "../models/ButtonProps.model";
import { PersonalLinksData } from "../models/PersonalLink.model";
import Button from "./Button.astro";

const links: PersonalLinksData[] = getPersonalLinks();
---

<div class="mb-10 mt-10 flex flex-col gap-8 px-8">
  <div class="flex flex-col items-center gap-8">
    <span class="text-4xl font-bold">Hello, I'm Flavio! 👋</span>
    <img
      src="images/cv_foto.JPG"
      alt="My photo"
      class="rounded-full"
      height="288"
      width="240"
    />
    <p class="text-lg">
      I am a Data Engineer at
      <a
        href="https://www.quantyca.it/"
        target="_blank"
        class="text-myLightBlue"
      >
        Quantyca
      </a>
      and an MSc graduate in Computer science and Engineering at
      <a href="https://polimi.it" target="_blank" class="text-myLightBlue">
        Polimi
      </a>
      with a passion for innovation and problem-solving. I'm skilled in cloud development,
      software engineering, algorithms, web development and have a strong track record
      of delivering powerful results through collaboration and ongoing learning.
    </p>
  </div>
  <div
    class="flex flex-row justify-between px-10 md:w-full md:justify-start md:gap-4 md:px-4"
  >
    {
      links.map((link) => (
        <>
          <Button
            text=""
            href={link.linkUrl}
            icon={link.icon}
            isFilled={true}
            color={ButtonPropsColor.PRIMARY}
            classes="md:hidden"
            ariaLabel={link.ariaLabel}
          />
        </>
      ))
    }
    {
      links.map((link) => (
        <>
          <Button
            text={link.linkName}
            href={link.linkUrl}
            icon={link.icon}
            isFilled={true}
            color={ButtonPropsColor.PRIMARY}
            classes="hidden md:block md:w-1/3"
            ariaLabel={link.ariaLabel}
          />
        </>
      ))
    }
  </div>
  <div class="flex w-full flex-row md:px-4">
    <Button
      text="Need a new website? Contact Me!"
      href="mailto:flaviorizzoglio@gmail.com"
      textSize={ButtonTextSize.BIG}
      isGradient={true}
      color={ButtonPropsColor.SECONDARY}
      classes="w-full hidden md:block"
      ariaLabel="Contact me"
    />
    <Button
      text="Need a new website? Contact Me!"
      href="mailto:flaviorizzoglio@gmail.com"
      isGradient={true}
      color={ButtonPropsColor.SECONDARY}
      classes="w-full md:hidden"
      ariaLabel="Contact me"
    />
  </div>
</div>
