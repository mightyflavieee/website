---
import { ButtonPropsColor, IconSide } from "../models/ButtonProps.model";
import ButtonDownload from "./ButtonDownload.astro";

const cvButtonIcon = {
  name: "ic:baseline-cloud-download",
  side: IconSide.LEFT,
};
---

<nav
  class="sticky mt-2 flex flex-row justify-between px-2 text-center md:mt-4 md:px-10"
>
  <a href="/"><img src="/images/logo.gif" alt="Logo" class="h-12 w-12" /></a>
  <ButtonDownload
    text="RESUME"
    href="/Flavio_Rizzoglio_CV.pdf"
    icon={cvButtonIcon}
    isGradient={true}
    color={ButtonPropsColor.SECONDARY}
  />
</nav>
